{
  "name": "secxion",
  "version": "1.0.0",
  "scripts": {
    "install": "cd backend && npm install",
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && npm start"
  },
  "dependencies": {
    "concurrently": "^9.1.2"
  }
}
